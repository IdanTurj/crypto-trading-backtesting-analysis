# Sample price data

`sample_prices.csv` contains a small synthetic-style historical close-price series used to make the portfolio project reproducible.

The backtest only requires two columns:

- `timestamp`
- `close`

The sample is intentionally small. Larger historical datasets can be substituted as long as the `close` column is present.
